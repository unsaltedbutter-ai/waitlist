# Shared Nostr config — used by both nostr-bot and update-checker
# Lives at: ~/.unsaltedbutter/nostr.env
# Generated by: deploy.sh --setup-bots (DATABASE_URL auto-filled)

# ── Identity ──────────────────────────────────────────────────
NOSTR_NSEC=nsec1...                    # NEVER commit real key
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.snort.social

# ── Update checker ────────────────────────────────────────────
OPERATOR_NPUB=***REDACTED***

# ── Nostr bot profile (published as kind 0) ───────────────────
BOT_NAME=UnsaltedButter Bot
BOT_ABOUT=DM me to manage your streaming rotation. Zap me to add credits.
BOT_LUD16=bot@getalby.com             # Lightning address for receiving zaps

# ── Zap validation (NIP-57) ──────────────────────────────────
# The nostr pubkey your Lightning provider uses to sign kind 9735 zap receipts.
# For Alby: curl https://getalby.com/.well-known/lnurlp/bot | jq -r '.nostrPubkey'
ZAP_PROVIDER_PUBKEY=deadbeef...        # 64-char hex, NOT npub

# ── Database ──────────────────────────────────────────────────
# Auto-filled by --setup-bots from ~/.unsaltedbutter/db_password
DATABASE_URL=postgresql://butter:PASSWORD@localhost:5432/unsaltedbutter

# ── Logging ───────────────────────────────────────────────────
LOG_LEVEL=INFO
