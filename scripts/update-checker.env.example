# Update Checker — Nostr DM config
# Copy to ~/.update-checker.env and fill in values

# Bot's Nostr secret key (reuse nostr-bot key)
NOSTR_NSEC=nsec1...

# Operator's npub to receive DMs (default: your npub)
OPERATOR_NPUB=***REDACTED***
