# ==================================================
# shared.env.example
# Copy to ~/.unsaltedbutter/shared.env
# Loaded by: orchestrator, nostr-bot, agent (on Mac Mini)
# ==================================================

# ---- Nostr identity (one key for bot + orchestrator) ----
NOSTR_NSEC=nsec1...
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.snort.social

# ---- Public keys ----

# Operator's npub (bech32, receives alerts and admin commands)
OPERATOR_NPUB=npub1...

# VPS private bot pubkey (hex, verifies push DMs from VPS)
VPS_BOT_PUBKEY=

# LNURL/zap provider pubkey (hex, validates zap receipts per NIP-57)
ZAP_PROVIDER_PUBKEY=

# ---- VPS connection ----

# VPS API base URL (HMAC-authenticated agent endpoints)
API_BASE_URL=https://unsaltedbutter.ai

# HMAC shared secret (must match AGENT_HMAC_SECRET on VPS)
AGENT_HMAC_SECRET=

# Public site URL (used in login/invite links)
BASE_URL=https://unsaltedbutter.ai

# ---- Bot profile (published as kind 0) ----
BOT_NAME=UnsaltedButter Bot
BOT_ABOUT=DM me to manage your streaming services. Pay-per-action, 3k sats.
BOT_LUD16=

# ---- Common settings ----
LOG_LEVEL=INFO
OTP_TIMEOUT_SECONDS=900
