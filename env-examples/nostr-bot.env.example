# ==================================================
# nostr-bot.env.example
# Copy to ~/.unsaltedbutter/nostr-bot.env
# Loaded after shared.env (overrides shared values if needed)
# Runs on: Mac Mini
# ==================================================

# All nostr-bot variables come from shared.env.
# Use this file only if you need per-component overrides,
# e.g. a different LOG_LEVEL for the bot vs orchestrator.
