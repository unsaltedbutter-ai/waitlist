# Bot identity
NOSTR_NSEC=nsec1...                    # NEVER commit real key
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.snort.social

# Bot profile (published as kind 0)
BOT_NAME=UnsaltedButter Bot
BOT_ABOUT=DM me to manage your streaming services. Pay-per-action, 3k sats.
BOT_LUD16=bot@getalby.com             # Lightning address for receiving zaps

# Zap validation (NIP-57)
# The nostr pubkey your Lightning provider uses to sign kind 9735 zap receipts.
# For Alby: curl https://getalby.com/.well-known/lnurlp/bot | jq -r '.nostrPubkey'
ZAP_PROVIDER_PUBKEY=deadbeef...        # 64-char hex, NOT npub

# VPS API (HMAC-authenticated agent endpoints)
API_BASE_URL=https://unsaltedbutter.ai
AGENT_HMAC_SECRET=your-hmac-secret-here

# VPS private bot (accepts push notifications from this pubkey)
VPS_BOT_PUBKEY=                        # 64-char hex of the VPS private Nostr bot

# Operator (npub that can run admin commands like "invites")
OPERATOR_NPUB=npub1...

# Public site URL (used in login/invite links sent via DM)
BASE_URL=https://unsaltedbutter.ai

# Logging
LOG_LEVEL=INFO
