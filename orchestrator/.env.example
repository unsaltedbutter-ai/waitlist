# ==================================================
# orchestrator/.env.example
# Copy to ~/.unsaltedbutter/orchestrator.env (preferred)
# or orchestrator/.env (fallback)
# Runs on: Raspberry Pi or Mac Mini (NOT the Mac Studio)
# ==================================================

# ---- Required (no defaults) ----

# VPS API base URL
API_BASE_URL=https://unsaltedbutter.ai

# HMAC shared secret (must match AGENT_HMAC_SECRET on VPS)
AGENT_HMAC_SECRET=

# Bot's Nostr secret key (nsec1...)
NOSTR_NSEC=

# VPS private bot pubkey (hex, used to verify push DMs from VPS)
VPS_BOT_PUBKEY=

# LNURL/zap provider pubkey (hex, used to verify zap receipts)
ZAP_PROVIDER_PUBKEY=

# Operator's npub (bech32, receives alerts)
OPERATOR_NPUB=

# ---- Optional (sensible defaults) ----

# Nostr relays (comma-separated)
# NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.snort.social

# Website base URL
# BASE_URL=https://unsaltedbutter.ai

# Mac Mini agent endpoint (LAN address)
# AGENT_URL=http://192.168.1.100:8421

# Callback server bind address (agent posts results here)
# CALLBACK_HOST=0.0.0.0
# CALLBACK_PORT=8422

# Local SQLite path for orchestrator state
# DB_PATH=orchestrator.db

# Max parallel jobs dispatched to the agent
# MAX_CONCURRENT_AGENT_JOBS=2

# Price per cancel/resume action (sats)
# ACTION_PRICE_SATS=3000

# Seconds to wait for user to provide OTP code (15 min)
# OTP_TIMEOUT_SECONDS=900

# Seconds before unpaid invoice marks job as reneged (24h)
# PAYMENT_EXPIRY_SECONDS=86400

# Minimum seconds between outreach DMs to the same user (48h)
# OUTREACH_INTERVAL_SECONDS=172800

# Timer loop tick interval (seconds)
# TIMER_TICK_SECONDS=60

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Bot Nostr profile metadata
# BOT_NAME=UnsaltedButter Bot
# BOT_ABOUT=DM me to manage your streaming services. Pay-per-action, 3k sats.
# BOT_LUD16=
